import styled from 'styled-components';
import { warrior } from '../../../../utils/color';

export default {
  HotelCard: styled.div`
    margin: 20px;
    max-width: 40%;
    min-width: 250px;
    background: ${warrior};
  `,
};
